<%- include('layout') %>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card mt-5" style="border-color: #007bff; border-width: 2px;">
                <div class="card-body">
                    <% if (message) { %>
                        <h1 class="card-title">Server error</h1>
                        <p class="card-text"><%= message %></p>
                    <% }  %>
                </div>
            </div>
        </div>
    </div>
</div>
